{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A weather app built with React and Node",
  "main": "server/app.js",
  "scripts": {
    "start": "node server/app.js",
    "dev": "concurrently \"cd client && npm start\" \"cd server && nodemon app.js\"",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "concurrently": "^6.4.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
